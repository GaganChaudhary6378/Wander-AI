# Copy to terraform.tfvars and set values. Do not commit terraform.tfvars if it contains sensitive data.

aws_region         = "ap-south-1"
state_bucket_name  = "ai-tfstate"
lock_table_name   = "ai-tfstate-lock"
